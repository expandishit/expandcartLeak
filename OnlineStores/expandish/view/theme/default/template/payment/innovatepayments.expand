{% if error %}
    {{ error }}
{% else %}
    <form action="{{ action }}" method="post" id="payment_form">
        {{ fields }}
        <div class="buttons"><div class="right"><button type = 'submit' class="button"><span>{{ lang('button_confirm') }}</span></button></div></div>
    </form>
{% endif %}

