        <!-- box band -->
        <div class="container" id="section-{{ section_id }}">
		<div class="box-band block3">
            {% for collection in collections %}
            <a href="{{ collection.Man_Link.field_value }}"><img src="{{ collection.Man_Image.field_value }}" alt=""></a>
            {% endfor %}
        </div>
    </div>
        <!-- ./box band -->